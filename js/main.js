var React = require('react');
var ReactDOM = require('react-dom');


var App = React.createClass({
  render() {
    return (
      <div> HALLO WORLD </div>
    );
  }
});

React.render(new HelloWorld(), document.)

ReactDOM.render(
  <App />
);

